# Filters v2

This directory contains the v2 implementation of the filters specification supporting Proxy2 and the updated filters provided by Envoy.

There is an example of the noop filter implementation in the `example` directory.