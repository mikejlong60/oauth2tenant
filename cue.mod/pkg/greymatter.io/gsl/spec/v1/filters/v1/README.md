# Filters v1

This directory contains the v1 implementation of the filters specification.

V1 is currently divided into http and network filters. Implementations for each filter can be found in the `http` and `network` directories.
